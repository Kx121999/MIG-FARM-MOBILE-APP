# MIG FARM: تقرير تنفيذ Phase 1

تم تعديل المشروع الحالي، دون مشروع جديد أو مسارات جديدة أو تغيير خدمات التجارة والدفع والمساعد.

## 1. الملفات المعدلة

- `app.json`: مراجع أيقونات التطبيق وشاشة البداية فقط.
- `app/_layout.tsx`: تهيئة تجربة الدخول والحركة والاتصال مع بقاء Stack ومساراته.
- `app/(tabs)/_layout.tsx`: شريط التنقل الحالي.
- `app/(tabs)/index.tsx`: الرئيسية.
- `app/(tabs)/catalog.tsx`: تنسيق المتجر والبحث وحالات التحميل والنتائج الفارغة.
- `app/(tabs)/cart.tsx`: حالة السلة الفارغة فقط.
- `src/constants/theme.ts` و`src/contexts/LanguageContext.tsx`.
- المكونات القائمة: `AppHeader`, `CategoryCard`, `ProductCard`, `ScreenState`, `SectionTitle`.
- الأصول الجديدة: `assets/home-farm.webp`، ست صور `category-*.webp`، و`icon-mig-farm.png`, `adaptive-icon-mig-farm.png`, `splash-mig-farm.png`.
- أدوات التحقق: `scripts/prepare-brand-assets.mjs`, `scripts/verify-phase1.test.mjs`.

## 2. مكونات قابلة لإعادة الاستخدام

داخل `src/components`: `AppButton`, `AppIconButton`, `CountBadge`, `BrandLogo`, `MotionPressable`, `Skeleton`, `ProductRail`, `EmptyState`, `AppEntry`, `Onboarding`, `ConnectivityProvider`, `ConnectionNotice`.
أضيف `src/utils/appEntry.ts` لعزل حفظ اكتمال التعريف واختباره. لم تضف مكتبات تشغيل جديدة.

## 3. نظام التصميم

خلفية هادئة `#F7F8F6`، أسطح بيضاء، أخضر رئيسي `#175C3B` وأسعار `#B43C3C`. مسافات موحدة 4/8/12/16/24/32/48، زوايا الكروت 8، أزرار أساسية بارتفاع 48 وأهداف لمس 44. سلم خط من 12 إلى 28 مع line-height مناسب وletterSpacing صفر. لا تدرجات أو مؤثرات زجاجية جديدة.

## 4. شاشة البداية

إعداد `expo-splash-screen` بخلفية بيضاء ولوجو رسمي بعرض 200 و`contain`. إخفاء الشاشة الأصلية مرتبط باستعداد التخطيط/الشعار. شريط الحالة داكن ليظل مقروءا. انتقال الشاشة الأصلية يحتاج مراجعة على build مثبت فعليا.

## 5. Brand Intro

كشف خفيف للشعار مدته 850ms في أول دخول، مع تكبير 1.02 فقط وfade. يتم تجاوزه عند تفعيل تقليل الحركة وللمستخدم العائد. لا دوران أو bounce أو جسيمات.

## 6. Onboarding

ثلاث شاشات بالنصوص العربية المطلوبة وترجمة إنجليزية، التالي والسابق ومؤشرات الصفحات والتخطي والسحب الأفقي. المحتوى قابل للتمرير رأسيا للشاشات القصيرة وتكبير الخط. التخزين عبر AsyncStorage بمفتاح مستقل `mig_farm_onboarding_v1`؛ مهلة قصوى ثانيتان تمنع تعليق الدخول عند تعطل التخزين. لا تسجيل إجباري أو طلب أذونات. أول دخول والإكمال والعودة تم اختبارها؛ وتم اختبار التخطي مع رابط منتج مباشر دون فقدان الوجهة.

## 7. الرئيسية

هيدر مختصر، بحث يصل لمسار البحث القائم، Hero واحد، أقسام مصورة، وصل حديثا ومختارات ميغ فارم، ومدخل هادئ للمساعد. المنتجات المشاهدة أخيرا تظهر عند توفر سجل. الصور المحلية مشتقة من أصول المشروع؛ صورة Hero أصبحت نحو 131KB بدلا من نحو 2.4MB، وصور الأقسام نحو 6-25KB.

## 8. التنقل

الرئيسية / المتجر / المساعد / السلة / حسابي. جميع المسارات القديمة محفوظة، ومنها البحث، مع شارة السلة ومراعاة insets وإخفاء الشريط عند ظهور لوحة المفاتيح على الهاتف. ترتيب عربي من اليمين وإنجليزي من اليسار.

## 9. كروت المنتجات

مكون مشترك للرئيسية والمتجر والبحث والترشيحات. صورة `contain` بارتفاع ثابت 180، اسم بسطرين، سعر واضح وسعر سابق عند وجود خصم حقيقي، مفضلة ومقارنة محفوظتان، وإضافة صغيرة. ارتفاع الكارت 350 عند حجم الخط الافتراضي ويتمدد مع تكبير الخط. الصور تستخدم التخزين المؤقت المتاح في React Native، مع skeleton وبديل محايد عند فشل الصورة. صور الأقسام تبدأ بصورة البيانات ثم الأصل المحلي دون emoji.

## 10. الحركة والنجاح

استجابة ضغط 160ms بمقياس 0.985 واحترام reduced motion. علامة نجاح ورسالة لقارئ الشاشة عند الإضافة، وشارة السلة تتحدث دون فتحها. Expo Haptics Light على الأجهزة الأصلية فقط. تمت إزالة تحذير pointerEvents من المكون الجديد.

## 11. حالات المحتوى

Skeleton للكروت، حالة سلة فارغة مع بدء التسوق، بحث بلا نتائج مع مسح البحث، ورسائل اتصال/تحميل مفهومة مع إعادة المحاولة في المكون المشترك، دون كشف نص الاستثناء. تنبيه الاتصال الفوري يعتمد على أحداث المتصفح في Web؛ على Native تظهر حالة الاتصال عند فشل الطلب، وليس مراقبا دائما للشبكة.

## 12. ملفات الأيقونة

ملفات PNG مربعة 1024x1024 بخلفية بيضاء مع الحفاظ على نسبة الشعار. ملف splash مطابق لبكسلات الشعار الأصلي. لم يعد الرسم العام السابق مستخدما كأيقونة التطبيق.

## 13. أيقونة Android

نعم، الشعار الرسمي مستخدم في الأيقونة العامة وadaptive foreground. اختبار بكسلات تحقق من بقاء الرسم داخل دائرة الأمان وعدم تغيير الشعار. الهوامش متعمدة لحماية الاسم الكامل من قص launcher.

## 14. أيقونة iOS

نعم، تستخدم الشعار الرسمي عبر `expo.icon`، بتكوين معتم ومتمركز، دون شفافية أو تمديد أو تغيير ألوان العلامة.

## 15. الهوية والإعدادات

الاسم محفوظ: **MIG FARM | ميغ فارم**. Android package وiOS bundleIdentifier كلاهما `com.migfarm.app`. رابط Render وaiApiUrl وStripe وEAS project linkage لم تتغير. `preview`, `ios-simulator`, `production` و`submit` محفوظة. اختبارات SHA-256 تؤكد عدم تغيير EAS وخدمات API/AI/payments وCommerceContext وcheckout/orders وملف خادم backend.

## 16. مراجعة RTL/LTR

تمت مراجعة الواجهة والتفاعل باللغتين، مع قياس ترتيب الألسنة وصحة `document.dir`، والبحث والسلة وأسماء المنتجات الطويلة. مقاسات CSS المقاسة خلال المراجعة تشمل 358x569 و360x739 و430x931 دون overflow أفقي في الصفحات المختبرة. هذه محاكاة متصفح وليست مراجعة أجهزة Android/iPhone أصلية. قيود نافذة المتصفح لم تسمح بتأكيد 320px فعليا.

## 17. التحقق الفني

- `npm.cmd run typecheck`: ناجح.
- `node --test scripts/verify-phase1.test.mjs`: ثمانية اختبارات ناجحة. يحتاج `sharp` لأعمال فحص الأصول فقط، من المسار المعين في `MIG_SHARP_PATH` أو من البيئة المحلية.
- `expo config --type prebuild --json`: ناجح؛ مراجع الأصول والـplugins صالحة.
- `expo export --platform all --output-dir dist-phase1 --max-workers 1 --no-bytecode`: ناجح للـWeb وAndroid وiOS. هذا تصدير حزم JavaScript وأصول، وليس APK/IPA أو بناء EAS موقعا.
- فحص Render: `/health` و`/api/products` استجابا بنجاح، وعرض المتجر 250 منتجا. لم تنفذ عملية شراء أو دفع حقيقي.
- تم اختبار الإضافة السريعة وتحديث الشارة والسلة والحذف وحالة الفراغ، البحث بلا نتائج ومسحه، الوصول للمساعد دون إرسال رسالة، وصفحة المنتج بعد تخطي التعريف.

## 18. ملاحظات متبقية قبل النشر

- فحص `EXPO_NO_CACHE=1 expo install --check` عبر الإنترنت أبلغ عن إصدارات تصحيح أحدث: Expo 57.0.19، React Native 0.86.3، وتحديثات لـmetro-runtime/constants/haptics/image-picker/linking/router/splash-screen. لم تعدل dependencies أو lockfile؛ ترقية SDK والتحقق من مكتبات الدفع يجب أن تتم كعمل منفصل. الفحص غير المتصل السابق ليس بديلا عن هذا التحذير.
- لم يتم بناء APK/IPA أو تشغيل iOS Simulator أو تثبيت نسخة على جهاز. اختبار splash الحقيقي وlauncher masks وnotch/home indicator والاهتزاز وتكبير الخط والسحب باللمس ما زال مطلوبا على أجهزة فعلية.
- منطق fallback للصورة وفشل الاتصال تمت مراجعته، وظهرت حالة فشل تحميل أثناء اختبار الاتصال ثم نجحت إعادة المحاولة. لم تتوفر محاكاة شبكة بطيئة/انقطاع قسري أو اعتراض صورة فاشلة داخل المتصفح الحالي، فلا تعد هذه اختبارات أجهزة مكتملة.
- جودة الشعار الأصلية محفوظة؛ تحويله إلى canvas أكبر لا يضيف تفاصيل مفقودة. لم يتم توليد شعار بديل.
- هذه مرحلة الواجهة وتجربة الدخول، وليست إعادة تصميم كاملة للمساعد أو checkout أو الحساب. لم تعدل منطق تلك الميزات.

## المعاينة

السيرفر الحالي: http://127.0.0.1:8097/
هذا رابط معاينة محلي على الكمبيوتر؛ التطبيق ما زال يتصل بالـProduction Backend كما كان. تغيير native icon/splash يتطلب build جديدا ليظهر في نسخة الهاتف المثبتة.
