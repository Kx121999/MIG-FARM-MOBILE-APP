# MIG FARM: Customer Foundation
تاريخ المراجعة: 2026-09-03

## النتيجة
تم التنفيذ داخل المشروع الحالي، بدون مشروع ثانٍ أو تغيير محرك الدفع أو خادم المتجر أو AI أو EAS.
تجربة الضيف والعناوين والمفضلة والسجل والتفضيلات المحلية تعمل. الحساب الحقيقي والمزامنة ورفع الصور تحتاج Backend غير موجود حاليًا.
هذه نسخة تطوير قابلة للمعاينة، وليست شهادة جاهزية للنشر التجاري.

## التقرير المطلوب
1. **الملفات الجديدة:** نماذج `src/types/customer.ts`، خدمات `customer.ts / sessionStore.ts / avatar.ts / orders.ts`، أدوات `utils/customer.ts / utils/orders.ts`، سياقا `AuthContext / RetentionContext`، `useCustomerAddresses`، ومكونات `src/components/account/*` والاختبارات `scripts/customer-domain.test.mjs`.
2. **الملفات المعدلة:** `app/_layout.tsx`، `app/(tabs)/account.tsx`، `app/(tabs)/index.tsx`، `app/profile.tsx`، `app/orders.tsx`، `app/notifications.tsx`، `app/checkout.tsx`، `src/components/AppHeader.tsx`، `src/contexts/CommerceContext.tsx`، `src/types/index.ts`، `app.json`، `.env.example`، `package.json`، `pnpm-lock.yaml`، واختبار حماية المرحلة السابقة.
3. **المسارات الجديدة:** `/auth/login`، `/auth/register`، `/auth/forgot-password`، `/addresses`، `/favorites`، `/recently-viewed`، `/order-detail`، `/settings`، `/support`، `/legal`. المسارات السابقة محفوظة.
4. **الضيف:** التصفح والشراء والمفضلة والسجل والدعم والمساعد متاحون دون تسجيل. بيانات الضيف القديمة ومفاتيح تخزينها محفوظة.
5. **بنية المصادقة:** AuthService typed + AuthContext + تحقق نماذج + أخطاء محلية مفهومة. واجهات للبريد والهاتف والاستعادة والتجديد والخروج وApple/Google مستقبلًا. لا يوجد OAuth أو OTP حقيقي الآن.
6. **Backend المصادقة:** غير موجود. كل العمليات غير المدعومة ترجع `unavailable` ولا تنشئ حسابًا وهميًا أو ترسل كلمة مرور إلى عنوان مخترع.
7. **لوحة الحساب:** ترحيب مضغوط، إجراءات الطلبات والعناوين والمفضلة والإشعارات، ثم الملف والمساعد والدعم والإعدادات واللغة والوثائق.
8. **الملف الشخصي:** الاسم والبريد والهاتف والإمارة واللغة. للضيف بيانات تواصل على الجهاز بوضوح، وليست هوية حساب موثقة.
9. **الصورة:** اختيار ومعاينة واستبدال المعاينة وإلغاؤها. الحفظ والحذف الدائمان مهيآن لواجهة الخدمة ولا يُعرض نجاح بدون رد خادم.
10. **المعرض والكاميرا:** عبر Expo Image Picker وبطلب المستخدم فقط. لا طلب إذن أثناء Splash أو Onboarding أو أول Home.
11. **القص والضغط:** قص مربع حيث يدعمه النظام؛ الحفاظ على نسبة الصورة، JPEG بجودة 0.85 وحد أقصى 512 بكسل للضلع الأطول. رفض المصدر الأكبر من 25MB عند توفر الحجم. Web يعرض معاينة دائرية ولا يوفر محرر قص تفاعلي.
12. **تخزين الصورة:** MISSING. يلزم رفع مؤمّن وتخزين وURL/مرجع مرتبط بالمستخدم وحذف للملف القديم. لا تخزين دائم لبيانات base64.
13. **الهيدر العام:** UserAvatar وAccountAvatarButton مشتركان داخل AppHeader؛ 36 بكسل للصورة و44 للمساحة التفاعلية. الشعار والإشعارات والسلة محفوظة.
14. **المستخدم المسجل:** الصورة أو الحرف الأول من الاسم عبر حالة واحدة. الواجهة مهيأة؛ السلوك بحساب حقيقي غير مختبر لغياب Backend.
15. **الضيف:** أيقونة مستخدم محايدة. الضغط يفتح حسابي، لا يجبره على Login.
16. **Fallback:** رابط HTTPS فقط؛ فشل الصورة يعود للحرف الأول أو أيقونة محايدة، دون وجوه مولّدة.
17. **الحالة والكاش:** تحديثات الملف والصورة مركزية، placeholder أثناء الاستعادة، version من avatarUpdatedAt للروابط العادية. الروابط الموقعة لا تُعدّل؛ الخادم مسؤول عن إصدار رابط جديد عند التغيير. حماية من رد ملف متأخر بعد الخروج.
18. **العناوين:** إضافة وتعديل وحذف بتأكيد واختيار افتراضي، مع ترقية عنوان آخر عند حذف الافتراضي. أنواع منزل/مزرعة/شركة/آخر. كلمة مزرعة هنا تصنيف عنوان فقط.
19. **Checkout:** يعرض العناوين المحفوظة ويملأ العنوان والوحدة والملاحظات وبيانات المستلم عند الاختيار؛ الإدخال اليدوي والضيف مستمران. لم تتغير Stripe/createCheckoutSession.
20. **الطلبات:** سجلات الضيف الموجودة على نفس الجهاز، تحميل خمس طلبات في الصفحة، ملخص وصور وتفاصيل. لا مزامنة حقيقية عبر الأجهزة.
21. **الحالات:** طبقة mapping للحالات الفعلية فقط: awaiting_payment / paid / payment_failed / canceled. الحالة غير المعروفة تظهر كغير متاحة. لا تجهيز/توصيل/GPS وهمي.
22. **إعادة الطلب:** جلب كتالوج حديث دون fallback قديم، مطابقة المنتج والمتغير تحديدًا، السعر الحالي، دمج الكميات، وعرض غير المتاح قبل تأكيد إضافة المتاح للسلة. لا يفتح السلة تلقائيًا.
23. **المفضلة:** صفحة مستقلة مرتبطة بأزرار المنتجات الحالية، إضافة/إزالة وحالات فارغة. تبقى محلية؛ مزامنتها واجهة خدمة وقاعدة دمج فقط.
24. **شوهد مؤخراً:** آخر ثمانية منتجات من التدفق الحالي، صفحة مستقلة ومسح بتأكيد، بدون إنشاء سجلات غير حقيقية.
25. **الإشعارات:** شاشة وفلاتر وحالات فارغة/خطأ وواجهة تحميل صفحات عند توفر الخدمة. مركز الإشعارات الحقيقي غير متاح الآن.
26. **التفضيلات:** تحديثات الطلب والعروض والمنتجات الجديدة والتوفر؛ محلية مع معالجة فشل الحفظ. العروض والجديد مغلقان افتراضيًا، وبيانات الإصدارات القديمة لا تُعد موافقة تسويقية.
27. **الدعم:** رقم الشركة الموجود أصلًا مركزي في company.ts، واتساب/اتصال/الفروع وأسئلة قصيرة. لم يُخترع رقم جديد ولم تُرسل رسالة.
28. **المساعد:** روابط من الحساب والدعم إلى المساعد الحالي. عنوان AI ومنطق خدمته لم يتغيرا.
29. **التخصيص:** مختارات خفيفة من فئة آخر منتج تمت مشاهدته، مع مفتاح إيقاف. الاكتشاف العام محفوظ؛ لا استنتاج أن المنتج مناسب لمزرعة المستخدم.
30. **الأمان:** access token في الذاكرة، refresh token على Native في SecureStore مع حماية النسخ الاحتياطي؛ كتابات متسلسلة حتى لا يتجاوز حفظ قديم عملية الخروج. Web في الذاكرة فقط إلى أن تتوفر جلسة HttpOnly على Backend. لا تسجيل كلمات مرور أو tokens.
31. **الخصوصية:** الصورة للحساب فقط؛ لا تحليل وجه أو إرسالها للمساعد. روابط الخصوصية والشروط قابلة للضبط عبر EXPO_PUBLIC_PRIVACY_URL وEXPO_PUBLIC_TERMS_URL، وتعرض عدم التوفر عند غياب الوثائق الرسمية.
32. **حذف الحساب:** كلمة المرور الحالية ثم توضيح وتأكيد وطلب Backend. يجب أن يحذف الخادم الصورة المرتبطة ويلغي الجلسات وفق سياسة معتمدة. لم تُنفذ عملية حذف فعلية.
33. **الخدمات الموجودة:** health، products، product detail، media، checkout session، guest order detail، Stripe webhook.
34. **الخدمات الناقصة:** مصادقة العميل وملفه وصوره وعناوينه ومزامنة المفضلة وسجل الطلبات والإشعارات وحذف الحساب. المصفوفة أدناه.
35. **التحقق:** TypeScript نجح؛ 20 اختبارًا نجح؛ Expo prebuild config نجح؛ تصدير حزم Web/iOS/Android نجح. التصدير تحقق JavaScript/assets وليس APK/IPA أو build موقّع.
36. **القيود:** التفاصيل أدناه؛ لم تُبنَ أي شاشة My Farm أو أدوات حساب زراعية، ولا يُدّعى اكتمال Backend.

## Backend Reality Check
التصنيف هنا للخدمة على الخادم، وليس وجود واجهة محلية لها.

| الخدمة | الحالة | الواقع |
|---|---|---|
| REGISTER | MISSING | لا endpoint |
| LOGIN | MISSING | لا endpoint |
| LOGOUT | MISSING | لا إلغاء جلسة على الخادم |
| SESSION REFRESH | MISSING | لا refresh endpoint |
| FORGOT PASSWORD | MISSING | لا تدفق إرسال/تأكيد |
| USER PROFILE | MISSING | لا سجل عميل أو endpoint |
| PROFILE PHOTO UPLOAD | MISSING | لا رفع/تخزين صور حسابات |
| PROFILE PHOTO DELETE | MISSING | لا حذف من التخزين |
| ADD ADDRESS | MISSING | الإضافة المحلية فقط تعمل |
| EDIT ADDRESS | MISSING | التعديل المحلي فقط يعمل |
| DELETE ADDRESS | MISSING | الحذف المحلي فقط يعمل |
| ORDER HISTORY | PARTIAL | مراجع طلبات الجهاز فقط، لا قائمة طلبات عميل |
| ORDER DETAIL | AVAILABLE | GET /api/orders/:id مع Bearer token خاص بالطلب |
| FAVORITES SYNC | MISSING | لا مزامنة عبر الأجهزة |
| NOTIFICATIONS | MISSING | لا feed ولا Push delivery |
| NOTIFICATION SETTINGS | MISSING | تفضيلات الجهاز فقط |
| DELETE ACCOUNT | MISSING | لا حذف حساب أو صورة أو إلغاء جلسات |

تمت مراجعة مصدر الخادم الحالي. لا يمكن استنتاج حالة نشر مسارات إضافية غير موجودة في هذا المصدر.
فحص القراءة المباشر على Production أعاد 200 لـ /health و/api/products (250 منتجًا) وتفاصيل منتج وصورته.
لم يُنفذ شراء أو دفع أو كتابة بيانات عملاء على Production.

## QA الفعلي
- Home/Account كضيف، دخول الحساب من avatar، اللغة الإنجليزية والعربية.
- Login: تحقق البريد ورسالة الخدمة غير المتاحة دون نجاح وهمي.
- Register وForgot Password: فحص الشاشات. إرسال التسجيل من المتصفح لم يُستكمل بسبب فحص أمان الإجراء؛ اختبارات الخدمة تؤكد أن adapter الحالي يرفض التسجيل دون اتصال شبكة أو إنشاء حساب. لا حاجة لإنشاء حساب إنتاجي للاختبارات المحلية.
- Profile: رفض البريد غير الصحيح؛ اختيار صورة اختبار محلية ومعاينتها وإلغاء المعاينة؛ تأكيد أن الحفظ الدائم غير متاح.
- العناوين: إضافة وتعديل وحذف بتأكيد وحفظ عبر إعادة التحميل؛ تعبئة العنوان في Checkout. اختبار تغيير/حذف الافتراضي وتمييز رقم الهاتف ضمن unit tests.
- المفضلة: إضافة، فتح صفحة المفضلة، إزالة وحالة فارغة.
- السجل: منتج شوهد فعليًا، فتح السجل ثم المسح بالتأكيد.
- السلة: إضافة فعلية في الواجهة، تحديث العداد، دخول Checkout واختيار عنوان دون متابعة الدفع، ثم حذف عنصر الاختبار.
- الإشعارات: التسويق مغلق، تفضيل التوفر محفوظ بعد إعادة التحميل ثم إعادته للإغلاق.
- الدعم والسياسة القانونية المفقودة والإعدادات: الوصول وحالات الضيف سليمة.
- عروض CSS المختبرة: 360x739، 390x845، 430x931. صور فعلية للشاشة وفحص scrollWidth أظهرا عدم overflow أفقي في الحالات المفحوصة.
- حماية EAS وAI وخدمات الدفع والكتالوج والخادم بالأحجام/البصمات، واختبارات الشعار وSplash وOnboarding، نجحت.

## القيود المطلوبة قبل Production Accounts
- لا يوجد Backend حسابات؛ تفعيل flag وحده لا يكفي. يلزم تنفيذ adapters بعقود مصادقة موثقة وربط طلبات العميل بالجلسة، ownership checks، revocation، verification وrate limiting.
- دمج guest -> account مهيأ كنموذج وخدمة وخوارزمية idempotent مختبرة، لكنه غير مفعل على Login بدون Backend. يلزم ربط المزامنة وفصل تخزين بيانات كل حساب وعدم تسريب بيانات حساب سابق. السلة والمفضلة الحالية محلية.
- صلاحيات الكاميرا والمعرض، القص Native، SecureStore على جهاز فعلي، جلسة حقيقية، تحديث avatar عالمي حقيقي، تعطل صورة حساب حقيقية، Logout/Delete Account، Push، وطلبات عميل حقيقية لم تُختبر end-to-end.
- رموز طلبات الضيف في تدفق Checkout السابق لا تزال ضمن AsyncStorage. لم تُغيّر هذه المرحلة ترحيلها حتى لا تفقد المراجع الحالية؛ يلزم ترحيلها لتخزين آمن على Native ومعالجة استمرار طلبات Web قبل اعتماد أمان الإنتاج. رموز المصادقة الجديدة لا تستخدم AsyncStorage.
- سجل الطلبات مرتبط بالجهاز؛ فقد التخزين يفقد المراجع. خطأ جلب أحد طلبات الصفحة يُظهر إعادة المحاولة ولا يختلق بيانات.
- رفع الصورة وحذف الصورة القديمة وحذفها مع الحساب يجب تنفيذهم على الخادم بملكية المستخدم وتحقق MIME/الحجم وتنظيف الملفات. معاينة Web مؤقتة فقط.
- يلزم إدخال وثائق الخصوصية والشروط الرسمية؛ لا يوجد نص قانوني مختلق.
- لم تُنفذ محاكاة Network offline/throttling في المتصفح الحالي. ميزات الضيف تعتمد على التخزين المحلي والكتالوج الاحتياطي الحالي؛ الطلبات تحتاج الشبكة.
- لم تُختبر أجهزة iPhone/Android حقيقية أو Simulator أو VoiceOver/TalkBack أو Dynamic Island أو لوحة مفاتيح Native. فحص المتصفح لا يغني عنها.
- توجد تحذيرات peer dependencies في الشجرة الحالية: react-native-worklets 0.11.4 مقابل مجال expo-modules-core، وmetro-config 0.87.0 مقابل community-cli-plugin 0.86.2. لم تُجرَ ترقية شاملة للـSDK في هذه المرحلة.
- سكربت verify-bugfix القديم يعتمد على مسارات متصفح محلية ويتضمن تجهيز دفع؛ لم يُشغّل على Production. استخدمت مراجعة المتصفح الحالية دون إرسال دفع.
- مفاتيح Stripe الحقيقية ليست جزءًا من التسليم؛ إعدادات الدفع الحالية محفوظة ولا يعني نجاح التصدير أن الدفع الإنتاجي مفعّل.

## تشغيل النسخة
من مجلد المشروع بعد تثبيت Node والتبعيات:
```powershell
pnpm.cmd install --frozen-lockfile
npm.cmd run typecheck
node --test scripts/customer-domain.test.mjs
npx.cmd expo start
```
اختبار verify-phase1 يتطلب sharp المتاح في بيئة المراجعة (أو ضبط MIG_SHARP_PATH إلى تثبيته).
للمعاينة الحالية أثناء تشغيل الكمبيوتر: http://127.0.0.1:8098/account
Production API: https://mig-farm-api.onrender.com
AI API وEAS واسم الحزمة وbundleIdentifier والأيقونات وSplash محفوظون.

