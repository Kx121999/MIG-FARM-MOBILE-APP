# MIG FARM - Smart My Farm OS

## حالة التنفيذ

تمت إضافة نظام "مزرعتي" إلى المشروع الحالي من دون تغيير مسارات المتجر أو منطق الدفع أو إعدادات الإنتاج. التسوق يظل متاحا للضيف، بينما بيانات المزرعة تتطلب حسابا مسجل الدخول وترتبط دائما بصاحب الحساب على الخادم.

## الملفات والطبقات

- قاعدة البيانات: `server/db/migrations/002_my_farm_os.sql`.
- خدمة الخادم: `server/services/farms.mjs`.
- مسارات API: مدمجة في `server/src/app.mjs`.
- الأنواع والخدمة: `src/types/farm.ts` و`src/services/farm.ts`.
- الحالة والمزامنة: `src/contexts/FarmContext.tsx`.
- واجهات النظام: `app/my-farm/*` ومكونات `src/components/farm/FarmUI.tsx`.
- اختبارات التكامل: `server/tests/my-farm-api.test.mjs`.

## ما تم تنفيذه

1. مزارع متعددة مع الاسم والنوع والإمارة والمنطقة والمساحة ووحدتها والتحويل إلى متر مربع كقيمة قياسية.
2. مناطق زراعية وبيوت محمية مع نوع التربة ونظام الري ومصدر المياه.
3. دورات محصول مرتبطة بالمزرعة والمنطقة، وحالة ومرحلة نمو يحددها المستخدم يدويا.
4. لوحة يومية تعرض المهام المستحقة والمتأخرة، المحاصيل النشطة، المشكلات والسجلات الأخيرة.
5. تقويم اليوم والأسبوع والشهر والقادم والمتأخر.
6. مهام متكررة يوميا أو أسبوعيا أو كل عدد أيام، مع سجل إكمال فعلي.
7. سجلات ري فعلية للمدة والحجم والطريقة من دون حساب احتياج مائي مفترض.
8. عمليات زراعية وتسميد ومعاملات موصوفة بوضوح كسجلات أدخلها المستخدم وليست توصيات.
9. مركز مشكلات مع الشدة والحالة والمتابعة (أفضل/كما هي/أسوأ) والحل وإعادة الفتح والتاريخ الكامل.
10. جلسات تشخيص تحفظ الاحتمالات منفصلة عن التشخيص المؤكد، ولا تسمح للذكاء الاصطناعي بكتابة تشخيص نهائي.
11. مخزون مزرعة بكميات ووحدات يسجلها المستخدم، مع حذف منطقي يحفظ سلامة التاريخ.
12. تحاليل تربة ومياه بقيم المختبر الفعلية فقط، من دون تفسير أو جرعات آلية.
13. حصاد بوحدات محددة أو وحدة مخصصة، وربطه بدورة المحصول.
14. ملاحظات وبحث موحد في المزارع والمناطق والمحاصيل والمشكلات والعمليات والملاحظات.
15. تقرير فترة يعرض حقائق السجلات، ويحتفظ بواقعة حل المشكلة حتى لو أعيد فتحها لاحقا.
16. Cache منفصل لكل مستخدم، وطابور Offline للمهمات والري والعمليات والمشكلات والحصاد والملاحظات.
17. إعادة إرسال آمنة بمفتاح `Idempotency-Key` ومنع تكرار السجل عند إعادة المزامنة.
18. واجهة عربية RTL وإنجليزية LTR، خلفية هادئة وبطاقات قليلة وحدود خفيفة.
19. دخول واضح من الرئيسية والحساب، مع بوابة تسجيل دخول خاصة بمزرعتي فقط.
20. إرسال سياق مختار فقط إلى مساعد المزرعة، من دون إرسال كل السجل الخاص تلقائيا.

## الأمان والملكية

- كل استعلام خاص بالمزرعة مقيد بـ`user_id` مع فحص ملكية المزرعة والعلاقات التابعة.
- توجد Foreign Keys وفهارس وقيود تحقق على الحالات والوحدات.
- حذف المزرعة والمنطقة والمحصول والمشكلة والمخزون منطقي حيث يلزم الحفاظ على التاريخ.
- اختبارات مستقلة تثبت أن مستخدما آخر لا يستطيع قراءة أو تعديل بيانات المزرعة.
- لا تخزن الصور أو الرموز السرية في AsyncStorage.

## الواقع التشغيلي

- تخزين صور المزرعة وتقارير المختبر يحتاج Object Storage خارجي. حاليا يرجع الخادم بوضوح `farm_media_storage_not_configured` ولا يدعي نجاح رفع وهمي.
- الطقس يحتاج مزود طقس خارجي. حاليا تعرض الـAPI `weather_provider_not_configured`.
- Push Notifications تحتاج مزود إشعارات وإعداد مفاتيح المنصات، لذلك لم يتم ادعاء تفعيلها.
- التوصيات الرقمية الزراعية والجرعات لا تصدر من هذا النظام قبل وجود مصدر معتمد ومراجعة مهندس.
- نشر الميزة يتطلب تشغيل Migration الجديدة ثم نشر Backend وتطبيق Expo المحدثين.

## أوامر النشر

```powershell
pnpm db:migrate
pnpm typecheck
pnpm test
```

بعد نشر الخادم، تحقق بحساب مصادق من `/api/my-farm/dashboard`. مسار `/health` ومسارات المتجر العامة تبقى كما هي.

## نتائج التحقق

- TypeScript: ناجح.
- اختبارات المشروع: 59 من 59 ناجحة.
- اختبارات My Farm: 9 من 9 ناجحة.
- Expo Doctor: 21 من 21 ناجحة.
- إعدادات API وAI وEAS وStripe ومعرفات iOS/Android والأيقونات محفوظة.

## حدود معروفة

- الصور والملفات، الطقس، Push Notifications ومراجعة مهندس حقيقية تحتاج خدمات خارجية لم تقدم مفاتيحها بعد.
- بيانات "مزرعتي" لن تعمل على Backend الإنتاج الحالي حتى تنشر Migration وكود الخادم الجديد.
- لا توجد نتيجة صحة مزرعة مصطنعة، ولا جداول ري أو تسميد مخمنة، ولا تشخيص آلي نهائي.
