# MIG FARM: تحديث الحسابات وبداية التطبيق

## حالة التسليم

هذا Patch للمشروع الحالي فقط، وليس مشروعًا جديدًا أو ملف تطبيق قابلًا للتثبيت. لم يُعد تصميم واجهة المتجر، ولم تُحذف الشاشات أو الصور السابقة. لم يتغير `app.json` أو `eas.json` أو خدمة المساعد أو الكتالوج أو إعداد Stripe الأصلي.

الكود منفّذ ومختبر محليًا، لكنه **لم يُنشر على Render ولم تُطبّق ترحيلاته على Neon**. آخر فحص للـAPI المنشور: `/health` أعاد 200 و250 منتجًا؛ `/api/me` أعاد 404. نجاح الاختبارات المحلية لا يعني تفعيل الحسابات في الإنتاج.

## نطاق التنفيذ

| # | البند | النتيجة |
|---|---|---|
| 1 | ملفات Backend الجديدة | `server/src/app.mjs`؛ مجلدات `server/auth` و`server/lib` و`server/services`؛ `server/db`؛ اختبارات `server/tests` |
| 2 | الملفات المعدلة | `server/src/server.mjs` أصبح نقطة تشغيل صغيرة؛ `package.json` وlockfile وقوالب البيئة ودليل التشغيل؛ ملفات ربط الواجهة الحالية |
| 3 | قاعدة البيانات | مخطط `mig_farm`: users، sessions، password_reset_tokens، user_addresses، user_favorites، orders، order_items، notifications، notification_preferences، stripe_events، rate_limits، schema_migrations |
| 4 | الترحيل | `pnpm db:migrate`؛ قفل transaction وchecksum لكل ترحيل، دون ترحيل تلقائي عند الطلبات |
| 5 | البيئة | DATABASE_URL، ORDER_TOKEN_SECRET؛ اختياريًا DATABASE_URL_UNPOOLED وAUTH_ACCESS_TTL وAUTH_REFRESH_TTL وTRUST_PROXY_HOPS وPASSWORD_RESET_BASE_URL؛ تبقى إعدادات Stripe الحالية |
| 6 | التسجيل | إنشاء مستخدم وجلسة وتفضيلات في transaction واحدة، بريد موحد وفريد، هاتف مطبّع، كلمة مرور آمنة |
| 7 | الدخول | بريد وكلمة مرور، خطأ invalid_credentials عام، لا SMS أو social login وهمي |
| 8 | تجديد الجلسة | access افتراضي 15 دقيقة وrefresh حتى 30 يومًا؛ تدوير refresh، وإلغاء العائلة عند إعادة استخدام رمز قديم |
| 9 | الخروج | logout وlogout-all وإلغاء الجلسات؛ مسح بيانات الجلسة المحلية عند الخروج |
| 10 | استعادة كلمة المرور | رموز عشوائية، hash في قاعدة البيانات، صلاحية 30 دقيقة، استخدام واحد، وإلغاء الجلسات بعد التعيين |
| 11 | إرسال البريد | غير مفعّل؛ adapter واضح يُرجع email_provider_not_configured؛ إعداد الرابط وحده لا يرسل بريدًا |
| 12 | الملف الشخصي | GET/PATCH /api/me، حقول مسموحة فقط؛ تغيير البريد مرفوض حتى إضافة تحقق منفصل |
| 13 | صورة الحساب | بيانات avatar محفوظة في النموذج؛ التخزين الخارجي غير مفعّل ويُرجع avatar_storage_not_configured؛ لا base64 داخل PostgreSQL |
| 14 | العناوين | إضافة وتعديل وحذف وتحديد افتراضي، مع عزل الملكية وdefault وحيد |
| 15 | الطلبات | PostgreSQL هو مصدر الحقيقة الجديد؛ أسعار وتفاصيل وعنوان العميل لقطة تاريخية ثابتة |
| 16 | شراء الضيف | محفوظ بدون فرض تسجيل الدخول؛ مسار GET /api/orders/:id يقبل رمز الطلب القديم والجديد، والرمز محفوظ كـhash على الخادم |
| 17 | طلبات الحساب | ربط customer_id من الجلسة فقط؛ قائمة paginated وتفاصيل لا يراها إلا صاحب الطلب |
| 18 | Stripe Webhook | التوقيع يُراجع على raw body؛ تحديث قاعدة البيانات داخل transaction، deduplication للأحداث، وعدم خفض طلب مدفوع بسبب حدث فشل متأخر |
| 19 | المفضلة | محلية للضيف، وخادم للمستخدم؛ دمج idempotent مع التحقق من وجود المنتج، دون تكرار أو إعادة دمج نفس اختيارات الضيف في كل دخول |
| 20 | الإشعارات | Inbox داخل التطبيق، pagination، read/read-all وتفضيلات مرتبطة بالحساب؛ لا Push delivery في هذه المرحلة |
| 21 | حذف الحساب | تأكيد كلمة المرور؛ إلغاء الجلسات وحذف الموارد الشخصية وإخفاء الهوية؛ فصل الطلبات مع إبقاء سجل المعاملات التاريخي |
| 22 | ربط الواجهة | apiClient موحّد، Bearer وتجديد مرة واحدة، منع ردود الحساب القديم بعد تبديل المستخدم، رسائل خطأ عربية وإنجليزية |
| 23 | SecureStore | refresh على iOS/Android في SecureStore؛ access في الذاكرة؛ لا auth tokens داخل AsyncStorage |
| 24 | انتقال الضيف | السلة والسجل المحلي لا يُحذفان عند الدخول أو فشل الجلسة؛ دمج المفضلة فقط، دون ادعاء مزامنة سلة أو امتلاك طلبات ضيف قديمة بالبريد |
| 25 | الحماية | scrypt بملح عشوائي وN=131072؛ مقارنة timing-safe؛ SHA-256 للرموز؛ SQL parameterized؛ قيود DB؛ rate limits؛ أخطاء بلا SQL أو أسرار |
| 26 | الاختبارات | `pnpm test`: نجح 50 اختبارًا للعميل والمجال والصور والإعدادات وHTTP/SQL، بما فيها مستخدم A/B وWebhook وcheckout وreset وحذف الحساب |
| 27 | TypeScript | نجح `pnpm typecheck` وفحص Expo public config؛ نجح تجميع JavaScript/Hermes لكل من Android وiOS مع الصورتين المضمّنتين، وليس بناء EAS أو تثبيتًا على جهاز |
| 28 | متطلبات النشر | ربط DATABASE_URL فعليًا، مراجعة المخطط الحالي، ترحيل الطلبات القديمة، secret ثابت، نشر Backend وتحديث التطبيق واختبار Stripe test على البيئة المنشورة |
| 29 | القيود | لا بريد أو avatar storage أو Push أو OTP مفعّل، ولا My Farm/Odoo/Admin؛ اختبارات الأجهزة الحقيقية والنشر على Neon/Render لم تُنفّذ |

## نشر آمن دون فقد الطلبات

1. خذ نسخة احتياطية من قاعدة البيانات الحالية، وافحص وجود مخطط `mig_farm` قبل التطبيق. لم تُفحص جداول Neon الحالية عبر اتصال SQL في هذه المهمة. الترحيل لا يحذف جداول قائمة؛ عند تعارض مخطط سابق يجب مراجعة التعارض، لا حذف البيانات.
2. **قبل استبدال الـBackend القديم**، صدّر ملفات `server/data/orders` الموجودة على Render إلى مكان آمن. أوقف إنشاء طلبات جديدة أثناء نقل المصدر، وأكمل استيراد الطلبات وتحقق من أعدادها قبل إعادة فتح checkout. لا تعتمد على بقاء القرص المؤقت بعد deploy.
3. استخدم Node حديثًا متوافقًا مع المشروع، وثبّت `pnpm install --frozen-lockfile` من جذر المشروع. ضع DATABASE_URL وORDER_TOKEN_SECRET في إعدادات خادم Render فقط. استخدم سرًا مولدًا عشوائيًا بطول 32 حرفًا على الأقل، لا النصوص الموجودة في fixtures الاختبار.
4. شغّل `pnpm db:migrate`. يمكن استخدام DATABASE_URL_UNPOOLED للترحيل، بينما يستخدم التشغيل connection pool محدودًا وTLS مع التحقق من الشهادة.
5. شغّل `pnpm db:import-orders -- /path/to/legacy-export`. الاستيراد يحافظ على أرقام الطلبات ورموز وصولها القديمة بعد hashing، ولا يحذف الملفات. راجع أي تعارض في PaymentIntent أو بيانات غير صالحة قبل المتابعة.
6. احتفظ بمفاتيح Stripe الحالية ونفس `/api/stripe/webhook`، ثم شغّل `pnpm server`. اختبر الشراء كضيف وكحساب، وصول Webhook، وبقاء الطلبات بعد إعادة تشغيل الخادم. عدم ضبط DATABASE_URL أو ORDER_TOKEN_SECRET يمنع checkout برسالة إعداد واضحة، ولا يرجع نجاحًا وهميًا.
7. اضبط CORS_ORIGIN للدومينات الفعلية، واضبط TRUST_PROXY_HOPS بعد التأكد من سلسلة وكلاء Render، حتى تعمل حدود المحاولات لكل عميل دون الثقة بترويسات مزورة.
8. شغّل `pnpm db:cleanup` دوريًا ضمن تشغيل الخادم/مهام الصيانة المعتمدة. لم يُنشأ أي automation أو cron في حسابك. لا تحذف أدلة refresh المعاد استخدامه قبل انتهاء عمر العائلة.
9. لا تغيّر ORDER_TOKEN_SECRET أثناء وجود محاولات دفع معلّقة؛ تغيير السر يحتاج خطة ترحيل للرموز، لأن إعادة محاولة checkout تُعيد اشتقاق رمز الوصول.

لا يحتاج التطبيق إلى أسرار PostgreSQL. يبقى `EXPO_PUBLIC_API_URL` هو الأولوية، مع عنوان الإنتاج الحالي fallback. لا يلزم تعديل aiApiUrl أو EAS أو package/bundle identifiers.

## الصورتان وبداية التطبيق

- الصورة الإنجليزية الأصلية `8cd0cdf2...png` محفوظة في `assets/launch/mig-farm-launch-en.png`؛ العربية الأصلية `ffb54266...png` في `assets/launch/mig-farm-launch-ar.png`. الاختيار مبني على محتوى الصورتين، وليس ترقيمهما المختلف في المستند.
- الصورتان مطابقتان byte-for-byte للأصل، 941×1672، دون تعديل أو إعادة توليد أو ترجمة أو قلب.
- لغة محفوظة أولًا، ثم لغة الجهاز: ar للعربية، وإلا en. لا تُرسم صورة محلية قبل انتهاء قراءة اللغة.
- Native splash المحايد كما هو، ثم `LocalizedLaunchScreen`؛ تحميل مسبق للملفين المحليين، contain ونسب صحيحة وsafe area، ظهور 900ms ثم fade 200ms مع احترام reduced motion.
- الصفحة التالية تُجهّز خلف الصورة، فلا يتطلب الانتقال انتظار Render أو الكتالوج، ولا fade إلى شاشة بيضاء. يبقى onboarding الحالي لأول مرة، وتظهر الصورة أيضًا عند الفتح البارد للمستخدم العائد.
- تم التحقق بصريًا من الصورتين، ومن تغيير العربية إلى الإنجليزية ثم العكس وإعادة الفتح؛ ومقاسات فعلية في المتصفح 358×569 و360×800 و390×845 و430×931، دون overflow. اختلاف نسبة الشاشة يترك مساحات هادئة بدل قص الشعار أو النص.
- اختبارات حسابية لحفظ كامل الرسم على 320×568 و360×800 و412×915 و390×844 و393×852 و430×932 مع هوامش notch/أسفل الشاشة. هذه ليست بديلًا عن اختبار notch/Dynamic Island وSecureStore على أجهزة حقيقية.
- تم اختبار الصور المحلية وأولوية اللغة في الكود؛ لم يتم محاكاة انقطاع شبكة جهاز حقيقي أو API بطيء على جهاز فعلي. التشغيل الأول لصفحة الويب نفسها يحتاج وصول ملفات الويب؛ offline الكامل المقصود هنا هو الأصول المضمّنة في التطبيق الأصلي.

## ملاحظات تشغيلية

- جلسة الويب الحالية في الذاكرة فقط؛ استمرار تسجيل دخول الويب بعد إعادة تحميل كاملة يحتاج لاحقًا cookies من نوع HttpOnly، ولا يوجد fallback غير آمن إلى localStorage. الهاتف يستخدم SecureStore للاستعادة.
- عند خروج المستخدم أثناء انقطاع الاتصال تُزال جلسته من الجهاز؛ لا يمكن ضمان وصول إلغاء الجلسة للخادم حتى يعود الاتصال. انتهاء الصلاحية يبقى مفروضًا على الخادم.
- سجلات الضيف المحلية القديمة وأرقام الطلبات تبقى كما كانت. الرموز الخاصة بطلبات الضيف ليست auth sessions، ولم نغيّر مخزنها المحلي في هذه المرحلة.
- حذف الحساب يبقي لقطة العميل والتوصيل داخل سجلات المعاملات. يجب اعتماد مدة الاحتفاظ والتنظيف المناسبة للمتجر قبل الإنتاج، ولا يُدّعى حذف كل أثر للمعاملة.
- إعادة تعيين كلمة المرور جاهزة في الـAPI وشاشة `/auth/reset-password`، لكن يلزم توصيل EmailDeliveryAdapter فعليًا واستضافة رابط HTTPS للشاشة؛ الرابط يستخدم fragment حتى لا يُرسل الرمز ضمن مسار طلب خادم الويب.
- اختبار SQL يستخدم PGlite، محرك PostgreSQL داخل اختبارات التطوير فقط. التشغيل الإنتاجي يستخدم pg وDATABASE_URL، وليس قاعدة بيانات بديلة أو JSON.
- لم تُجرَ عملية دفع حقيقية أو كتابة بيانات على قاعدة الإنتاج. واجهة المتجر والسلة وتفاصيل المنتج اختُبرت كضيف؛ مسار الحساب الكامل اختُبر عبر HTTP وSQL وعميل API المعزول، لا بحساب عميل إنتاجي.

مراجع التنفيذ: [node-postgres pooling](https://node-postgres.com/features/pooling) و[Node crypto](https://nodejs.org/api/crypto.html).
